__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
build/
dist/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Inter-shop message runtime artifacts (per-message YAML; not source).
/inbox/
/outbox/
.claude/

# git worktrees created by BC role subagents
.worktrees/

# pre-sync beads Dolt backup (recovery snapshot, kept per request)
.beads-dolt-backup-presync/
